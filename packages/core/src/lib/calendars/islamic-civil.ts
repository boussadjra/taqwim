import { createTabularCalendar } from './tabular'

/** Friday/civil tabular Hijri calendar (1 Muharram 1 AH = epoch day -492148). */
export const islamicCivil = createTabularCalendar('islamic-civil', -492_148)
