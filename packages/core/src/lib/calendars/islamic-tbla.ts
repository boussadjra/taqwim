import { createTabularCalendar } from './tabular'

/** Thursday/astronomical tabular Hijri calendar (one day before Civil). */
export const islamicTbla = createTabularCalendar('islamic-tbla', -492_149)
